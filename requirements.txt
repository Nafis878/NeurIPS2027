# Core. On Google Colab, torch + CUDA are preinstalled, so torch is left
# unpinned here to avoid clobbering the GPU build. transformers/scipy/etc are pinned-ish.
torch>=2.1
transformers>=4.40
scipy>=1.10
numpy>=1.24
matplotlib>=3.7
pyyaml>=6.0
pytest>=7.0
